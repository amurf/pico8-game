# Pico-8 Game ![](https://www.lexaloffle.com//gfx/p8b_pico8.png)

* `brew install watchman`
* `curl -L https://cpanmin.us | perl - --sudo App::cpanminus`
* `cpanm Template`
* `./build.pl` for single build or `./dev.sh` for auto rebuilds
